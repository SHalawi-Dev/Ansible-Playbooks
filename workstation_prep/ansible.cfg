[defaults]
#Inventory file has the host names which are supplied by DNS
inventory = inventory
#Private key for ssh
private_key_file = ~/.ssh/id_ed25519
#Remove deprecation warnings for now
deprecation_warnings=False
